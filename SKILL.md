---
name: law-doc-query
summary: 律师法律资料查询 - 支持法规/司法解释/裁判文书/理解适用多类型，本地+华宇元典+ima+其他法律数据库多源，强制溯源防幻觉
agent_created: true
read_when:
  - 查询法律法规条文
  - 查找司法解释规定
  - 查询裁判文书/案例
  - 引用法条或裁判原文
  - 查询理解与适用
  - 法律文档库检索
triggers:
  - 查XX法第X条
  - XX法关于XX的规定
  - 这条法律怎么理解
  - 帮我找关于XX的法条
  - 法条怎么适用
  - 有没有XX的案例
  - XX案怎么判的
  - 法院对XX怎么认为
  - 司法解释怎么说
---

# 法律文档库查询 Skill

## 适用场景
用户（律师）的法律资料分散在多个来源、多种类型中。本 skill 统一处理：

**文件类型（4类，自动识别）**
| 类型 | 说明 | 查询方式 |
|------|------|---------|
| normative | 法律法规、司法解释等规范性文件（含"第X条"结构） | article 按条号 / search |
| case | 裁判文书（判决书/裁定书，含案号、"本院认为"结构） | case 提取结构 / search --type case |
| commentary | 理解与适用、条文释义（解释性文本） | search / read |
| other | 其他 | search / read |

**数据源（4类）**
| 来源 | 访问方式 | 适用场景 | 权威性 |
|------|---------|---------|--------|
| 本地文档库 | `law_query.py` 脚本（`LAW_DOCS_DIR`） | 个人收集的 md 文件 | 取决于用户收集 |
| 华宇元典 | yuandian-mcp 连接器 | 法律法规/案例/裁判文书专业检索 | 高（专业数据库） |
| ima 知识库 | ima-mcp 连接器 | ima 中收藏的资料 | 取决于收藏来源 |
| 其他法律数据库 | 对应 MCP（如北大法宝/威科先行） | 专业法律检索（如已连接） | 高 |

## 环境配置
**本地库根目录**（两种方式，任选其一）：
- **方式A（推荐，灵活）**：不设环境变量，脚本自动用**当前工作目录**作为文档库。在包含法律文档库的 WorkBuddy 工作区里直接使用即可——agent 调用脚本时 cwd 即工作区，脚本能读到工作区内的 md 文件。
- **方式B（固定路径）**：设环境变量 `LAW_DOCS_DIR` 指向固定路径，`echo 'export LAW_DOCS_DIR="/path/to/你的法律文档库"' >> ~/.zshrc`。适合文档库不在工作区内的情况。

其他：
- 脚本路径：`~/.workbuddy/skills/law-doc-query/scripts/law_query.py`
- 运行方式：`python3 <脚本路径> <子命令> [参数]`（输出均为 JSON）
- **agent 调用注意**：若用方式A，确保在包含文档库的工作区目录运行；若工作区不含文档库，需先 `cd` 到文档库目录或改用方式B。
- 外部数据源：华宇元典（yuandian-mcp）和 ima（ima-mcp）均已连接；其他法律数据库按需在连接器管理启用。

## 可用工具（本地库子命令）
| 子命令 | 作用 | 关键参数 |
|--------|------|---------|
| list | 列出所有文档（含类型识别、条文号、案号） | 无 |
| outline | 输出某文件大纲 | <file> |
| search | 跨文件关键词搜索（带上下文） | <keyword> [--context N] [--type normative\|case\|commentary\|other] |
| article | 精确取某法某条原文（规范性文件） | <law_name> <article_no> |
| case | 查询裁判文书（提取案号/本院认为/判决结果） | <file> 或 --case-no <案号> |
| read | 读取文件指定行段或章节 | <file> [--from N] [--to M] [--section 标题] |
| verify | 核对指定行段是否包含某原文 | <file> --from N --to M --expect 片段 |

## 数据源路由（先判断查哪里）
```
用户提问
  │
  ├─ 问法条/司法解释条文 → 本地 normative（article）；本地无 → 华宇元典
  ├─ 问案例/裁判观点/某案怎么判 → 本地 case；本地无或要权威案例库 → 华宇元典
  ├─ 问理解与适用 → 本地 commentary + ima
  ├─ 模糊问题（"关于XX的规定"）→ 本地 search + 华宇元典 + ima（如已连接）
  └─ 不确定来源 → 先查本地 list，再按需扩展到元典/ima
```
**优先级**：本地（用户亲自收集，可信）> 华宇元典（专业权威，效力状态可靠）> ima（个人收藏）> 其他源。本地查到足够则不查外部；本地没有时优先查华宇元典，ima 作为补充。各源结果必须分别标注来源。

## 工作流（按问题类型分支）

### A. 查法条/司法解释（normative）
1. `article <law> <no>` 精确取条；不确定条号先 `outline <law>` 或 `search 关键词 --type normative`
2. `read` 取上下文（前后条文、附带的"理解与适用"段落）
3. `verify` 核对要引用的原文
4. 作答（区分法条原文 / 理解与适用 / 分析）

### B. 查案例/裁判文书（case）
1. 知道案号 → `case --case-no <案号>` 跨文件检索
2. 知道文件名 → `case <file>` 提取结构化字段（案号、本院认为、判决结果）
3. 按争议点找案例 → `search <争议关键词> --type case`，再对命中文书用 `case` 提取说理
4. 引用裁判原文 → `verify` 核对"本院认为"或"判决如下"段落
5. 作答（区分裁判原文 / 裁判要旨 / 分析）

### C. 查理解与适用（commentary）
1. `search <主题> --type commentary` 定位
2. `read` 读取释义段落
3. `verify` 核对原文
4. 作答（标注为"理解与适用"，不得表述为法条原文）

### D. 跨源查询（本地 + 华宇元典 + ima + 其他）
1. 先按 A/B/C 流程查本地
2. 本地不足时，按路由优先级查外部源（华宇元典→ima→其他）
3. 各源结果分别陈列，来源标签必须区分：`[本地:文件名:行号]` / `[元典:字段类型]` / `[ima:知识库名]` / `[其他源]`

## 防幻觉约束（硬规则，不可违反）

### 通用约束（所有来源适用）
1. **禁止凭自身知识回答法条/裁判内容**。任何"第X条规定了…""法院认为…"的陈述，必须先调用工具读取，且能给出来源标注。模型自身记忆的法条可能过时、裁判可能记错，一律以库内/外部源原文为准。
2. **找不到就说找不到**。工具返回空或未命中时，回答"未找到相关规定/案例"，不得用模型自身知识补全。
3. **法条原文 vs 理解与适用 vs 裁判原文 严格区分**。三者性质不同：
   - 法条原文：规范性，逐字引用不得改写
   - 理解与适用：解释性，是学者/机关对法条的阐释，不是法条本身
   - 裁判原文：个案说理，是特定法院对特定案件的观点，不具有普遍约束力（除最高法指导性案例）
   - 不得把解释当法条、把法条当意译、把个案观点当普遍规则
4. **引用必经 verify**（本地）。标注"[已核对]"的引用必须实际调用过 verify 且 `verified=true` **且 `unique=true`**（唯一匹配）；`unique=false`（区间内多处命中，套话/重复文本）时不得标"[已核对]"，须标"[未核对：非唯一匹配]"并缩小区间重核。未调用 verify 一律标"[未核对]"。本地 verify 无法核对外部源内容（见外部约束第11条）。
5. **多源不得混淆**。本地、华宇元典、ima、其他源的内容在回答中必须分别标注来源，不得笼统说"根据资料"。
6. **分析要标注性质**。你自己的法律分析必须放独立段落并明确标注"以下为分析，非原文"。

### 外部数据源通用约束（华宇元典 / ima / 其他法律数据库 MCP）
凡引用外部数据源（非本地库）的内容，除遵守上述6条外，还需：
7. **字段类型标注**。外部源返回结构化数据时，引用必须标注字段类型（法条原文/裁判要旨/案件评析/条文释义/摘要）。不得把"裁判要旨""案件评析"当原文逐字引用——要旨是法院提炼，评析是学者/编辑观点，只有"原文"字段才是规范文本。
8. **案例效力层级标注**。引用案例必须标注效力层级（指导性案例/公报案例/普通案例）。仅最高法指导性案例"应当参照"，公报案例具有参照效力，普通案例仅是个案观点。不得把普通案例的"本院认为"当普遍规则陈述。
9. **条文效力状态确认**。引用法条前必须确认效力状态（现行有效/已废止/已修改）。已废止条文不得当现行引用；如用户问的是已废止条文，明确提示"该条已废止"并说明替代条文（如外部源返回了替代信息）。
10. **原文优先**。外部源若同时返回原文和摘要/要旨，必须引用原文字段，不得引用摘要当原文。
11. **无原文定位标注**。外部源返回若仅摘要无原文定位（无法逐字核对），标注"[外部摘要，未逐字核对]"。本地 verify 子命令无法核对外部源内容，**不得假装已核对**——外部源引用一律不得标"[已核对]"，除非外部源返回了可逐字比对的原文且你已比对。
12. **跨源冲突处理**。外部源之间或外部源与本地冲突时——
    - 条文效力状态：以专业数据库（华宇元典/北大法宝）为准
    - 原文逐字：以本地核对为准（本地有则 verify，本地无则标注未核对）
    - 案例效力层级：以最高法发布为准
    - **效力状态降级**：若外部源标记某条文为"已修改/已废止"，即使本地有原文，本地原文也仅作"历史版本"参考，引用须以外部源现行有效文本为准，并明确提示"本地为修订前文本"
    - 冲突时明确告知用户存在差异，不擅自合并

## 输出格式

### 查法条时
```
## 法条原文
《XX法》第X条 [效力状态：现行有效/已废止]
[原文，逐字引用]
> 来源：[本地:文件名:行号] [已核对]
  或 [元典:法条原文字段] [外部摘要未核对]

## 理解与适用（如收录）
[解释内容]
> 来源：[本地:文件名:行号] 或 [元典:条文释义] 或 [ima:知识库名]

## 分析（如需要）
以下为分析，非原文：
[分析]
```

### 查案例时
```
## 案件信息
案号：XX  效力层级：指导性案例/公报案例/普通案例
> 来源：[本地:文件名] 或 [元典:案例字段] 或 [ima:知识库名]

## 本院认为（裁判说理原文）
[reasoning 原文]
> 来源：[本地:文件名:行号] [已核对]
  或 [元典:裁判文书原文字段] [外部摘要未核对]

## 裁判要旨（如外部源提供，须与原文区分）
[要旨内容——法院提炼，非文书原文]
> 来源：[元典:裁判要旨字段]

## 判决结果
[ruling 原文]
> 来源：[本地:文件名:行号] 或 [元典:判决字段]

## 分析（如需要）
以下为分析，非原文（注：本案为个案观点/指导性案例）：
[分析]
```

## 调用示例
```bash
SCRIPT=~/.workbuddy/skills/law-doc-query/scripts/law_query.py

# 列出所有文档（含类型）
python3 $SCRIPT list

# 查民法典第143条
python3 $SCRIPT article 民法典 143

# 按案号查裁判文书
python3 $SCRIPT case --case-no "（2023）京01民终123号"

# 查某裁判文书结构（提取本院认为/判决）
python3 $SCRIPT case 案例-张三李四

# 只在裁判文书里搜"不可抗力"
python3 $SCRIPT search 不可抗力 --type case --context 2

# 只在法规里搜"民事行为能力"
python3 $SCRIPT search 民事行为能力 --type normative

# 核对某裁判文书本院认为段落原文
python3 $SCRIPT verify 案例-张三李四房屋买卖合同纠纷.md --from 16 --to 17 --expect "不能预见、不能避免且不能克服"
```

## 外部数据源接入说明
- **华宇元典（yuandian-mcp）**：法律法规/案例/裁判文书专业检索，条文效力状态可靠。连接后 agent 在查法条/案例时自动调用。引用其内容须遵守外部约束第7-12条（字段类型/效力层级/效力状态/原文优先/未核对标注）。
- **ima 知识库（ima-mcp）**：个人收藏资料。连接后 agent 在查理解适用/补充资料时调用。
- **其他法律数据库**：如北大法宝（pkulaw）/威科先行（wk-workbuddy）等，连接后同样适用本 skill 的外部源约束。
- **未连接时**：只使用本地库；本地不足时告知用户"本地库未找到，可连接华宇元典/ima 扩展检索"。
- **来源标注**：外部源返回内容必须标注 `[源名:字段类型]`，与本地 `[文件名:行号]` 区分；摘要无定位时标 `[外部摘要，未逐字核对]`。
- **首次使用字段探测**：首次调用某个外部源（元典/ima/北大法宝等）前，先做一次探测调用（如元典的 `yuandian_list_apis` / 样本检索），记录返回字段名与含义，再按实际字段类型标注（法条原文/裁判要旨/案件评析/条文释义）。**不得臆测字段含义**——字段名与约束第7/10条的"原文 vs 摘要"判定直接相关，臆测会导致把要旨当原文引用。

## 边界情况
- 用户问的法律/案例本地没有 → 回答"本地库未收录"；如华宇元典/ima已连接则自动扩展查询，未连接则提示用户可连接
- 条号不存在 → 工具返回 available 条号清单，据此告知用户
- 案号查不到 → 告知"未找到该案号文书"，建议用关键词 search --type case 或查华宇元典
- 同一关键词多文档命中 → 全部列出，按文档分别作答，不混为一谈
- 文件类型识别错误 → 可在文件开头加 frontmatter `type: normative|case|commentary` 显式标注
- 引用与用户记忆不符 → 以本地 verify 结果为准（本地有）；外部源内容以原文优先、标注未核对
- 外部源返回内容与本地冲突 → 按约束第12条处理，告知用户差异，不擅自合并
